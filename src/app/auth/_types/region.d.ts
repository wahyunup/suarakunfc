export interface ProvincePayload {
  id: string;
  name: string;
}

export interface CityPayload {
  id: string;
  value: string;
  name: string;
}

export interface DistrictPayload {
  id: string;
  value: string;
  name: string;
}